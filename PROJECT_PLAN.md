# 🤖 Telegram Payment Bot - План разработки

## 📋 Обзор проекта

**Цель**: Создать Telegram бота для управления платными подписками в сообществе с ИИ-ассистентом на основе базы знаний.

**Основные функции**:
- 💳 Управление подписками через YooKassa
- 🎯 Гибкая система ценообразования  
- 🤖 ИИ-ассистент с базой знаний из сообщений группы
- ⚖️ Автоматическое управление доступом к группам

## 🗂️ Текущая структура проекта

```
pbot/
├── bot.py                    # Основной бот (СТАРЫЙ - использует Prodamus)
├── prodamus_api.py          # Устаревший Prodamus API
├── example/                 # 🟢 РАБОЧАЯ YooKassa интеграция
│   ├── yookassa_subscriptions.py    # Система подписок
│   ├── yookassa_webhook_handler.py  # Обработка вебхуков
│   ├── telegram_payments.py         # Telegram Payments
│   ├── yookassa_refunds.py         # Система возвратов
│   └── 0898.py                     # Основной бот с YooKassa
├── db.py                    # База данных (SQLite)
├── price_config.json        # Конфигурация цен
└── requirements.txt         # Зависимости
```

## 🎯 Этапы разработки

### Этап 1: Миграция на YooKassa ✅
**Статус**: Рабочий код есть в example/
**Задачи**:
- [x] Изучить рабочую YooKassa интеграцию из example/
- [ ] Перенести YooKassa код в основной проект
- [ ] Настроить автоматические подписки с сохранением карт
- [ ] Настроить webhook для обработки платежей
- [ ] Интегрировать систему напоминаний

**Файлы для переноса**:
- `yookassa_subscriptions.py` → система подписок
- `telegram_payments.py` → Telegram Payments интеграция
- `yookassa_webhook_handler.py` → обработка платежей

### Этап 2: Система ценообразования 💰
**Задачи**:
- [ ] Разные цены для новых/вернувшихся пользователей
- [ ] Установка скидок на текущий месяц админом
- [ ] Индивидуальные цены для конкретных пользователей
- [ ] Сохранение старой цены при продлении подписки
- [ ] Админ-панель управления ценами

**Структура ценообразования**:
```python
{
    "regular_price": 1800,      # Для новых пользователей
    "returning_price": 2000,    # Для вернувшихся
    "monthly_discount": 0.1,    # Скидка на месяц (10%)
    "individual_prices": {      # Персональные цены
        "user_id": price
    }
}
```

### Этап 3: Автоматизация управления группой 👥
**Задачи**:
- [ ] Система предупреждений перед исключением
- [ ] Автоматическое исключение неплательщиков
- [ ] Проверка статуса подписки при входе в группу
- [ ] Система реинвайтов после возобновления оплаты

**Логика исключений**:
1. За 4 дня до окончания - первое предупреждение
2. За 1 день - финальное предупреждение  
3. Через 2 дня после окончания - исключение из группы

### Этап 4: ИИ-ассистент с базой знаний 🧠
**Архитектура**: RAG (Retrieval-Augmented Generation)

**Компоненты**:
- **Сбор данных**: Сканирование сообщений в Telegram группах
- **Векторная БД**: Chroma/FAISS для хранения эмбеддингов
- **LLM**: g4f (GPT-4, GPT-4o, Claude) или Gemini 2.5 Flash
- **Поиск**: Семантический поиск по контексту

**Поток данных**:
```
Telegram сообщения → Обработка → Эмбеддинги → Векторная БД
                                                    ↓
Пользователь → Запрос → Поиск релевантного контента → LLM → Ответ
```

## 🔧 Технический стек

### Основные технологии
- **Bot Framework**: aiogram 3.x
- **Платежи**: YooKassa API + Telegram Payments
- **База данных**: SQLite → PostgreSQL (для продакшена)
- **ИИ**: g4f / Gemini 2.5 Flash
- **Векторная БД**: ChromaDB / FAISS
- **Веб-сервер**: aiohttp (для webhooks)

### Зависимости
```python
# Текущие
aiogram>=3.0.0
python-dotenv>=1.0.0
aiohttp>=3.8.5
yookassa>=3.0.0

# Для ИИ-ассистента (добавить позже)
g4f>=0.5.8.3
chromadb>=0.4.0
sentence-transformers>=2.2.0
langchain>=0.1.0
```

## 📊 База данных

### Таблица users (расширенная)
```sql
CREATE TABLE users (
    telegram_id BIGINT PRIMARY KEY,
    username VARCHAR(255),
    first_name VARCHAR(255),
    last_name VARCHAR(255),
    
    -- Подписка
    subscription_active BOOLEAN DEFAULT FALSE,
    subscription_end_date DATETIME,
    subscription_type VARCHAR(50), -- 'telegram_payments', 'admin_gift'
    tariff VARCHAR(50),
    
    -- Платежи
    payment_status VARCHAR(50),
    auto_renewal BOOLEAN DEFAULT FALSE,
    yookassa_payment_method_id VARCHAR(255),
    last_payment_date DATETIME,
    
    -- Ценообразование  
    personal_price INTEGER, -- Персональная цена (в копейках)
    price_locked_until DATETIME, -- До какой даты зафиксирована цена
    left_group BOOLEAN DEFAULT FALSE, -- Покидал ли группу
    
    -- Система напоминаний
    last_reminder_sent DATETIME,
    reminder_count INTEGER DEFAULT 0,
    
    -- Метаданные
    joined_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица knowledge_base (для ИИ)
```sql
CREATE TABLE knowledge_base (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message_id BIGINT,
    chat_id BIGINT,
    user_id BIGINT,
    content TEXT,
    message_date DATETIME,
    topic VARCHAR(255), -- Автоматически определенная тема
    embedding_vector BLOB, -- Сериализованный вектор
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_topic (topic),
    INDEX idx_date (message_date)
);
```

## 🔄 Рабочий процесс

### 1. Пользователь подписывается:
```
/start → Выбор тарифа → Определение цены → YooKassa платеж → 
Сохранение карты → Активация подписки → Добавление в группу
```

### 2. Автопродление:
```
Cron job → Проверка истекающих подписок → Списание с карты → 
Успех: Продление / Ошибка: Уведомление пользователя
```

### 3. ИИ-ассистент:
```
Пользователь → Вопрос → Поиск в векторной БД → Контекст + Запрос → 
g4f/Gemini → Генерация ответа → Отправка пользователю
```

## ⚙️ Конфигурация (.env)

```bash
# Telegram
TELEGRAM_API_TOKEN=your_bot_token
TELEGRAM_GROUP_ID=your_group_id
TELEGRAM_ADMIN_IDS=admin1,admin2,admin3

# YooKassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
PROVIDER_TOKEN=your_provider_token

# ИИ (один из вариантов)
G4F_MODEL=gpt-4o-mini
GEMINI_API_KEY=your_gemini_key (если используем Gemini)

# База данных
DATABASE_URL=sqlite:///bot_database.db

# Webhook
WEBHOOK_URL=https://your-domain.com/webhook
WEBHOOK_SECRET=your_webhook_secret
```

## 📋 TODO List по приоритетам

### Высокий приоритет (Этап 1)
- [ ] Перенести YooKassa интеграцию из example/
- [ ] Настроить автоматические подписки
- [ ] Настроить webhook обработку
- [ ] Протестировать платежи в тестовом режиме

### Средний приоритет (Этап 2-3)  
- [ ] Реализовать гибкую систему цен
- [ ] Добавить админ-команды управления
- [ ] Настроить автоматические напоминания
- [ ] Система исключения неплательщиков

### Низкий приоритет (Этап 4)
- [ ] Сбор сообщений для базы знаний
- [ ] Интеграция векторной БД
- [ ] Подключение g4f/Gemini
- [ ] Обучение и тестирование ИИ

## 🚀 Развертывание

### Локальная разработка:
1. `git clone` проекта
2. Создание `.env` с настройками
3. `pip install -r requirements.txt`
4. Настройка YooKassa тестового магазина
5. `python bot.py`

### Продакшн:
- VPS/VDS сервер (Ubuntu 22.04+)
- Nginx для проксирования webhooks
- PostgreSQL вместо SQLite
- SSL сертификаты для webhooks
- Systemd service для автозапуска

## 📈 Метрики и аналитика

### Отслеживание:
- Количество активных подписчиков
- Конверсия в платежи
- Средняя продолжительность подписки  
- Популярные темы в ИИ-ассистенте
- Эффективность системы цен

### Логирование:
- Все платежи и их статусы
- Ошибки автопродления
- Исключения пользователей
- Запросы к ИИ-ассистенту

---

## 📞 Следующие шаги

**Начать с**: Миграция на YooKassa (Этап 1)

**Приоритет**: Сначала настроить стабильные платежи, потом добавлять дополнительные функции.

**Тестирование**: Использовать YooKassa sandbox для всех тестов до релиза.

---

*Этот план будет обновляться по мере развития проекта. Сохраняйте этот файл для отслеживания прогресса.*